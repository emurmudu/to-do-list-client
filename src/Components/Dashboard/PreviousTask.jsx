import { Helmet } from "react-helmet";

const PreviousTask = () => {
    return (
        <div>
            <Helmet>
                <title>TaskManager | Previous task</title>
            </Helmet>
            <h1>Previous task page</h1>
        </div>
    );
};

export default PreviousTask;